<Window x:Class="Diplom_Stud.Components.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сообщение" Width="450" MinHeight="220" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" 
        Background="Transparent" WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False" Topmost="True">

    <Border Background="#12101C" BorderBrush="#2A283C" BorderThickness="1" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Opacity="0.5" ShadowDepth="0" Color="Black"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <Path x:Name="IconPath" Data="M12,2C6.48,2,2,6.48,2,12s4.48,10,10,10s10-4.48,10-10S17.52,2,12,2z M13,17h-2v-2h2V17z M13,13h-2V7h2V13z" 
                      Fill="#02B3BA" Width="20" Height="20" Stretch="Uniform" Margin="0,0,10,0"/>
                <TextBlock x:Name="txtTitle" Text="УВЕДОМЛЕНИЕ" Foreground="#FFFFFF" 
                           FontFamily="Digital Pixel" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1" x:Name="txtMessage" Text="Ваше сообщение здесь..." 
                       Foreground="#D1CDEF" FontFamily="Montserrat Thin" FontSize="16" 
                       TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancel" Content="НЕТ" Width="100" Height="40" Margin="0,0,15,0" 
                        Style="{StaticResource GhostButtonStyle}"
                        FontFamily="Digital Pixel" FontSize="12"
                        Click="Cancel_Click" Visibility="Collapsed"/>

                <Button x:Name="btnOk" Content="ОК" Width="120" Height="40" 
                        Cursor="Hand" Style="{StaticResource AuthModernButtonStyle}"
                        FontFamily="Digital Pixel" FontSize="10"
                        Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>